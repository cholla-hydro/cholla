include builds/make.type.hydro


POISSON_SOLVER ?= -DPARIS

#-- FIXME: Gravity build does not support GPU_MPI currently
GPU_MPI =

DFLAGS += $(POISSON_SOLVER)
DFLAGS += -DGRAVITY
DFLAGS += -DGRAVITY_5_POINTS_GRADIENT


DFLAGS += -DGRAVITY_GPU


ifeq ($(findstring shamrock,$(MACHINE)),shamrock)
DFLAGS += -DPARIS_NO_GPU_MPI
endif

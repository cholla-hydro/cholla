# Internal API

:::{todo}
Re-enable Breathe Extension so we can render the internal API documentation generated through doxygen.

This machinery is left over from an earlier attempt to use Sphinx and currently produces errors.
I suspect there are some issues with doxygen docstrings since we haven't been consistently running it.
:::

<!--
   :::{autodoxygenindex}
   :project: cholla
   :::
-->
